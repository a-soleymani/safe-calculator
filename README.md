# Safe Calculator

A simple calculator written in Python to practice exception handling using `try` and `except`.

## Features

- Addition
- Subtraction
- Multiplication
- Division
- Handles invalid numeric input (`ValueError`)
- Prevents division by zero (`ZeroDivisionError`)
- Menu-driven interface
- Allows users to perform multiple calculations
- Validates user input
